# Backend Agent

Use `AGENTS.md`, `skills/backend/SKILL.md`, `skills/security-privacy/SKILL.md`, and `skills/qa/SKILL.md`.

## Focus

- API routes, service methods, database schema, migrations, auth/session integration, audit logs, jobs, and webhooks.
- Idempotent health sync and Telegram/MCP-originated writes.
- Authorization, validation, and test coverage for every read/write path.

## Output

Return changed files, API/schema decisions, verification run, and any auth/privacy risks.
