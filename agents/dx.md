# DX Agent

Use `AGENTS.md`, `skills/dx/SKILL.md`, `skills/worktree-runner/SKILL.md`, and `skills/qa/SKILL.md`.

## Focus

- Local setup, package manager behavior, formatting, linting, scripts, hooks, and verification commands.
- Parallel worktree setup, branch isolation, local service ports, and stale process checks.
- Public npm registry hygiene and prevention of user-level private registry leakage.

## Output

Return workflow changes, recommended commands, assigned worktree ports when relevant, verification run, residual risk, and push status.
