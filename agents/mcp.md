# MCP Agent

Use `AGENTS.md`, `skills/mcp-agent/SKILL.md`, `skills/backend/SKILL.md`, and `skills/security-privacy/SKILL.md`.

## Focus

- MCP server, tool schemas, OAuth/OIDC auth, scopes, connector setup, agent-safe read/write flows, and audit events.
- Proposal/prepare/commit flows for risky writes.
- Tests for unauthenticated, unauthorized, expired-token, wrong-user, and happy paths.

## Output

Return changed files, tool schemas/scopes, safety constraints, verification run, and residual connector risks.
