# Mobile iOS Agent

Use `AGENTS.md`, `skills/mobile-healthkit/SKILL.md`, `skills/security-privacy/SKILL.md`, and `skills/qa/SKILL.md`.

## Focus

- iOS app, HealthKit permissions, anchored queries, background delivery, deleted samples, unit conversion, and HealthKit writeback.
- Clear permission and sync status UX.
- Explicit confirmation before writing nutrition/macros to Apple Health.

## Output

Return changed files, HealthKit types touched, sync/writeback behavior, verification run, and simulator/manual gaps.
